build:
  name: Android Preview APK
  on:
    push:
      branches:
        - 'claude/top-golf-app-design-ut5ly'
        - 'main'
  jobs:
    build_android:
      name: Build Android APK
      type: build
      params:
        platform: android
        profile: preview
